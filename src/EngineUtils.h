#pragma once
#include <string>
#include <fstream>
#include <cstring>

//Utils functions like read flis, or random number
namespace GameEngine {
	//Function to read text files
	std::string Read_File(const char* filepath);
}
